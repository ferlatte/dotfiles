#! /bin/bash

set -eu -o pipefail

pushd "${HOME}" > /dev/null
brew update
brew-upgrade-casks
brew bundle
# Apparently in brew land, --force means "Actually do what I asked you to do"
brew bundle cleanup --force
popd > /dev/null
